const DONGLE_MODE = {
  USB: 'usb',
  ETHERNET: 'ethernet'
};

const MQTT_MODE = {
  LOCAL: 'local',
  EXTERNAL: 'external'
};

const SETUP_MODES = {
  LOCAL: 'local',
  REMOTE: 'remote'
};

export { DONGLE_MODE, MQTT_MODE, SETUP_MODES };
